name: ✅Unit Testing Status Check
run-name: ✅Unit Testing Status Check ${{ github.base_ref == 'main' && '(Release Build)' || '(Debug Build)' }}


on:
  workflow_dispatch:
  pull_request:
    branches: main


jobs:
  run_tests:
    name: Unit Test Status Check
    uses: KinsonDigital/Infrastructure/.github/workflows/run-csharp-tests.yml@v8.1.0
    with:
      project-name: ${{ vars.PROJECT_NAME }}Tests
      build-config: "Debug"
      net-sdk-version: ${{ vars.NET_SDK_VERSION }}
